[package]
name = "matrix"
version = "0.1.0"
edition = "2024"
description = "Retro-futuristic Matrix digital rain with centered 3x5 text"
license = "Unlicense"

[dependencies]
crossterm = "0.27"
rand = "0.8"
clap = { version = "4.5", features = ["derive"] }
